package Task.mock;

import Task.ReferenceTask;
import Task.ResultListener;

public class ReferenceTaskMock implements ReferenceTask {
    @Override
    public void execute(String userId, ResultListener listener) {

    }
}
